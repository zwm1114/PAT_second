#include <stdio.h>

int main(){
	char origin[85] = {0};
	char actual[85] = {0};
	int arr[130] = {0};
	char a;
	int i = 0;
	int j = 0;

	scanf("%s",origin);

	while( a = getchar() != '\n'){
		if(a == origin[i]){
			actual[i] = a;
		}
		int[a] = 1;
		i ++;
	}

	for(j;j < i;j ++){
		if(int[actual[j]] == 0)
	}

	return 0;
}